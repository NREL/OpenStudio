

namespace openstudio {
  namespace filesystem {
    std::vector<uint8_t> read_file(const openstudio::path &t_path)
    {
      assert(false);
    }

    QByteArray read_file_as_QByteArray(const openstudio::path &t_path)
    {
      // implement me
    }
  }
}

