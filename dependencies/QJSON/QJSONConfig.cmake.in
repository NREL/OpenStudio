GET_FILENAME_COMPONENT(myDir ${CMAKE_CURRENT_LIST_FILE} PATH)

get_filename_component(QJSON_INSTALL_PREFIX "${QJSON_DIR}" ABSOLUTE)

SET(QJSON_LIBRARIES qjson)
SET(QJSON_INCLUDE_DIR "@INCLUDE_INSTALL_DIR@")

set(QJSON_LIBRARY_DIRS "${QJSON_INSTALL_PREFIX}/lib")
include(${myDir}/QJSONConfigVersion.cmake)
